name: Build and Deploy React App

on:
  push:
    branches: [ main ] # 监听 main 分支的推送

permissions:
  contents: write # 必须赋予权限，否则机器人没法帮你创建发布分支

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies 📦
        run: npm install

      - name: Run Tests 🧪
        run: npm run test # 重点：先安检！

      - name: Build 🔧
        run: npm run build

      - name: Deploy 🚀
        if: success() # 只有前面的步骤都成功了才执行
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages