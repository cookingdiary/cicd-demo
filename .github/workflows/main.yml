name: Build and Deploy React App

on:
  push:
    branches: [ main ] # 监听 main 分支的推送

permissions:
  contents: write # 必须赋予权限，否则机器人没法帮你创建发布分支

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build # 执行你的 webpack --mode production

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # 你的 Webpack 打包输出目录
          branch: gh-pages # 自动化部署的目标分支